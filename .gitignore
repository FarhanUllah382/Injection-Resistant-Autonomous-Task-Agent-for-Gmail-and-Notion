# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Anchored to repo root — unanchored `lib/`/`lib64/` (the standard Python
# template) also matched web/lib/, silently excluding real frontend source
# (found while adding web/lib/ in V2.7; nothing was ever committed there
# before now, so nothing was lost, but it needed fixing before this could
# work at all).
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Phase 1 outputs
phase1_extraction/evaluation_results.json

# OS
.DS_Store
Thumbs.db
