# URL of the FastAPI backend (app/main.py). The default matches `uvicorn
# app.main:app --reload`'s local port — only change this if the backend is
# running somewhere else.
NEXT_PUBLIC_API_URL=http://localhost:8000
