anthropic>=0.38.0
python-dotenv>=1.0.0

# Phase 2: backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlmodel>=0.0.22
psycopg2-binary>=2.9.9
google-auth>=2.35.0
google-auth-oauthlib>=1.2.1
google-api-python-client>=2.149.0
beautifulsoup4>=4.12.3

# Phase 4: Notion — official SDK for the single page-create call on approve
notion-client>=2.2.1

# V2.1: MCP client/server SDK — backend talks to mcp_servers/*/server.py over
# stdio (Design Decisions V2.1, Decision 3); pinned <2.0 for the stable
# FastMCP/stdio-client API (2.x renamed the server API and is very new)
mcp>=1.6.0,<2.0.0

# V2.7: in-process ~2-min Gmail poll inside the existing FastAPI process —
# avoids adding Celery/Redis; CLAUDE.md's "no background workers" default is
# deliberately superseded here (Design Decisions V2.7, Decision 1)
apscheduler>=3.10,<4.0
